# 🚗 سرویس مسیریابی با جنگو (Django OSRM Routing)

این پروژه یک میکروسرویس مسیریابی است که وظیفه‌ی محاسبه **کوتاه‌ترین مسیر (مسافت و زمان)** بین دو مختصات جغرافیایی را بر عهده دارد.  
موتور اصلی پردازش مسیرها، سرویس **OSRM** است و ارتباط با آن از طریق یک API تمیز، تفکیک‌شده و قابل نگهداری انجام می‌شود.

سرویس با استفاده از **Django + DRF** توسعه یافته و به‌طور کامل با **Docker** کانتینریزه شده است تا در هر محیطی به‌صورت یکسان اجرا شود.

---

## 🛠 پشته فناوری (Tech Stack)

- **Backend:** Django, Django REST Framework  
- **Routing Engine:** OSRM  
- **HTTP Client:** Requests  
- **Containerization:** Docker & Docker Compose  
- **Architecture:** Clean Architecture + Service Layer  

---

## ✨ ویژگی‌های کلیدی

- **API کامل و استاندارد:** دریافت مختصات و محاسبه سریع مسیر  
- **تفکیک لایه‌ها (Service Layer):** جلوگیری از شلوغی View و خوانایی بالا  
- **اعتبارسنجی حرفه‌ای:** با DRF Serializers برای مختصات جغرافیایی  
- **Clean Architecture:** امکان توسعه و نگهداری طولانی‌مدت  
- **کاملاً Dockerized:** اجرا فقط با یک دستور  
- **پردازش سریع:** اتصال مستقیم به OSRM Routing Engine  

---

## 🏗 ساختار پروژه (معماری)
برای رعایت اصول مهندسی نرم‌افزار، کدها در لایه‌های مجزا تفکیک شده‌اند:

* `api/services.py`: تمام منطق ارتباط با OSRM در این لایه است (جلوگیری از شلوغی View).
* `api/serializers.py`: اعتبارسنجی دقیق ورودی‌ها (مختصات جغرافیایی).
* `api/views.py`: فقط وظیفه دریافت درخواست و ارسال پاسخ استاندارد را دارد.
* `Dockerfile`: تنظیمات لازم برای اجرای پروژه در هر محیطی بدون وابستگی دستی.

---
## 🚀 راه‌اندازی و اجرا



### 🔥 اجرای سریع با Docker (پیشنهادی)
در مسیر اصلی پروژه، دستور زیر را اجرا کنید:
```bash
  docker-compose up --build
  
  پس از اجرا، سرویس روی آدرس زیر فعال است:
  
  http://localhost:8000
  
  ⚙️ اجرای دستی (بدون Docker)

  1. ساخت محیط مجازی
  python -m venv venv
  
  
  ویندوز:
  
  venv\Scripts\activate
  
  
  مک/لینوکس:
  
  source venv/bin/activate
  
  2. نصب وابستگی‌ها
  pip install -r requirements.txt
  
  3. اجرای سرور
  python manage.py runserver
  
  📡 مستندات API
  🧭 Endpoint
  
  URL: /api/shortest-path/
  
  Method: POST
  
  Content-Type: application/json
  
  🧪 مثال تست — مسیر تهران → اصفهان
  📨 درخواست
  {
      "start": [35.6892, 51.3890],
      "end": [32.6546, 51.6680]
  }
  
  
  استارت: تهران
  مقصد: اصفهان
  
  ✅ پاسخ موفق
  {
      "distance_km": 438.25,
      "duration_min": 291.2
  }
  
  ❌ خطای احتمالی
  {
      "start": ["Ensure this field has at least 2 elements."]
  }
  
  👨‍💻 توسعه‌دهنده
  
  محمد علی شعبانی آرا
  
  <p align="center"> ساخته‌شده با ❤️ برای پروژه‌های میکروسرویسی و مقیاس‌پذیر </p> ```
